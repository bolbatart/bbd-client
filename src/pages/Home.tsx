import React from "react";
import styled from "styled-components";

const Home: React.FC = () => {
  return (
    <StyledHome>
      <h4>Home page</h4>
    </StyledHome>
  );
};

export default Home;

const StyledHome = styled.div``;
